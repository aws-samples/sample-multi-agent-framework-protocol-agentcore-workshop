[project]
name = "data-quality-mcp-server"
version = "0.1.0"
description = "Data quality MCP server for the data pipeline ops workshop"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.0.0",
    "pyyaml>=6.0",
]
